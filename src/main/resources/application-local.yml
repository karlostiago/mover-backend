spring:
  application:
    name: mover
  datasource:
    url: jdbc:mysql://localhost:3306/mover?useSSL=true&serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 123456
    hikari:
      auto-commit: false
      connection-timeout: 15000
      max-lifetime: 600000
      maximum-pool-size: 20
      minimum-idle: 5
      leak-detection-threshold: 5000
      pool-name: master
  jpa:
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      "[hibernate.dialect]": org.hibernate.dialect.MySQL8Dialect
      "[hibernate.generate_statistics]": false
      "[hibernate.connection.provider_disables_autocommit]": true
  flyway:
    enabled: true
    url: jdbc:mysql://localhost:3306/mover?useSSL=false&serverTimezone=UTC&characterEncoding=UTF-8
    user: root
    password: 123456
    locations: classpath:db/migration
    database: mysql
  mail:
    host: smtp.gmail.com
    port: 465
    username: movertech59@gmail.com
    password: ohep ihxe ppcz nyjj
    properties:
      mail:
        smtp:
          ssl:
            trust: "*"
          auth: true
          starttls:
            enable: false
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory


mover:
  root-user: ${ROOT_USER}
  root-user-password: ${ROOT_USER_PASSWORD}
  secret-key: ${SECRET_KEY}

corpvs:
  base-url: ${CORPVS_BASE_URL}
  auth-path: /api/auth/login
  report-path: /api/report/daily_path
  registration-path: /api/registration/vehicle
  email: ${CORPVS_INTEGRACAO_EMAIL}
  password: ${CORPVS_INTEGRACAO_PASS}

parallelum:
  base-url: https://fipe.parallelum.com.br/api/v2
  car-path: /cars/brands
  reference-path: /references

viacep:
  base-url: https://viacep.com.br/ws/